kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: docker:dind
    volumes:
      - name: dockersock
        path: /var/run
      - name: build
        path: /drone/src/build
    commands:
      - export GOTIFY_VERSION=$(echo $GOTIFY_RELEASE_TITLE | awk '{print $2}')
      - echo "Building gotify version $GOTIFY_VERSION"
      - sleep 5 # give docker enough time to start
      - docker ps -a
      - pwd
      - apk --no-cache add build-base git go
      - export PATH=$PATH:$HOME/go/bin
      - make build-linux-amd64
      - make build-linux-arm64
      - make build-linux-arm-7
  - name: publish
    image: plugins/github-release
    volumes:
      - name: build
        path: /build
    settings:
      api_key:
        from_secret: github_api_key
      files:
        - /build/*.so
      title: "MQTT for Gotify ${GOTIFY_RELEASE_TITLE}"
      checksum:
        - md5
        - sha1
        - sha256
        - sha512
        - adler32
        - crc32

services:
  - name: docker
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run

volumes:
  - name: dockersock
    temp: {}
  - name: build
    temp: {}

trigger:
  event:
    - custom